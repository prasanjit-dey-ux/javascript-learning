// Note:
// here we are having a implicit scope, which means we are assuming there is already a scope
// its not a good way to write code.

let balance = 4000;

if (balance < 5000) console.log(`Balance is sufficient`)
